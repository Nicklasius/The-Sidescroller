class Skud {     // Classen med alle dens egenskaber
    constructor() {
        this.r = 40;
        this.x = x;
        this.y = y;
      
    }

    
    move() {
       this.x += 5;
    }

    show() {
        fill(50);
        rect(this.x, this.y, 50, 50);
    }
}